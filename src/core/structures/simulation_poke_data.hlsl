#ifndef SIMULATION_POKE_DATA
#define SIMULATION_POKE_DATA

/// This data structure stores the properties of a single simulation poke
typedef int SimulationPokeData[9]; // strength, radius, x, y, z, dx, dy, dz, mask

#endif
